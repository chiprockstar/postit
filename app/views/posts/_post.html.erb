<div class="row">
  <div class='span0 well well-sm text-center quiet'>
    <% if logged_in? %>
      <%= link_to vote_post_path(post, vote: true), method: 'post', remote: true do %>
        <i class='icon-arrow-up'></i>
      <% end %>
      <br/>
    <% end %>

      <span id="post_<%= post.slug %>_votes"><small><%= pluralize(post.total_votes, 'vote') %></small></span>
    <br/>
    <% if logged_in? %>
      <%= link_to vote_post_path(post, vote: false), method: 'post', remote: true do %>
        <i class='icon-arrow-down'></i>
      <% end %>
    <% end %>
  </div>
  <div class="span8 well">
    <%= render post.categories %>
    <h4><%= link_to post.title, fix_url(post.url) %></h4>
    <%= link_to 'edit', edit_post_path(post) %>
    <p><%= post.description %></p>
    <p>
    <span class="quiet">posted by</span> <%= link_to post.creator.username, user_path(post.creator) %>
    <small>at <%= display_datetime(post.created_at) %></small>
    </p>
    <small>
    <span>
      <%= link_to("#{pluralize(post.comments.size, 'comment')}", post_path(post)) %>
    </span>
   </small>
 </div>
</div>
